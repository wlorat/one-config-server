server:
  port: 8001

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/wlorat/one-config-server.git
          searchPaths: config-data
          username: wlorat@gmail.com
          password: 0607.William
  security:
    user:
      name: root
      password: s3cr3t